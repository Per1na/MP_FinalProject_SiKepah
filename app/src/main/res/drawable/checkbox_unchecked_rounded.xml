<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Warna checkbox yang tidak dicentang -->
    <corners android:radius="25dp" /> <!-- Membuat sudut melengkung (bulat) -->
    <size android:width="24dp" android:height="24dp"/>
    <stroke android:color="@color/hitam" android:width="2dp"/>
</shape>
